{
  "name": "pramukh-alpha-crm",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev",
    "build:backend": "npx prisma generate --schema=backend/prisma/schema.prisma && cd backend && npm run build && node ../scripts/copy-api-dist.mjs",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@vercel/node": "^3.2.29",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.16",
    "serverless-http": "^3.2.0",
    "uuid": "^11.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
